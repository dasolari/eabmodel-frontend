import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    maxWidth: 345,
  },
  infoContainer: {
    textAlign: 'left',
  },
  media: {
    height: 300,
  },
});

export default useStyles;
